# Backend base URL. WS URL is derived by swapping http→ws.
# Local dev:
VITE_API_BASE=http://localhost:8000
# Production (Vercel): set this in the Vercel project's Environment Variables
# to your deployed Cloud Run backend, e.g.
#   VITE_API_BASE=https://arenapulse-backend-xxxxx.run.app
